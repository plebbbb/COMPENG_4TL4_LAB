clear all
tiledlayout(3,1)

data = zeros([30,1]);
data(16) = data(16) + 1;
data(12:end) = data(12:end) + 1;

unitimpulse = @(array, point) (array(point:end) = )
unitstep = @(array, start) (array(point:end))

nexttile
stem(data)
title("Unit impulse and unit step")

nexttile

